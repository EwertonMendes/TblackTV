(function bootstrapNamespace(global) {
  'use strict';

  global.SportsHub = global.SportsHub || {
    config: {},
    core: {},
    services: {},
    adapters: {},
    ui: {},
    controllers: {}
  };
}(window));
